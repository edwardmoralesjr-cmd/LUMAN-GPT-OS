# LUMAN OS v2 (skeleton)

A clean-slate re-execution of LUMAN OS. The old system fused **structure,
state, content, and interface** into prose Markdown, which made it drift and
require constant manual syncing. v2 separates them.

## The layers

| Layer | Lives in | Who maintains it |
|---|---|---|
| **Structure** — sections, commands, modules, routes | `luman.json` | you (one file, source of truth) |
| **State** — open loops, priorities | `state/*.json` | the runtime / you |
| **Content** — prose for each section & module | `sections/*.md`, `modules/*.md` | you |
| **Interface** — home screen, menus | *generated by `luman.py`* | nobody — it's rendered |
| **Intelligence** — readings, canon checks, copy | an LLM, called into module slots | — |

The menus can't drift because they're never written by hand — they're rendered
from the manifest every run.

## Commands

```
python3 luman.py home      # render the live home screen
python3 luman.py loops     # list open loops
python3 luman.py next      # the single highest-leverage next move
python3 luman.py open <id> # open a section (dashboard, books, harmonic, gpt-lab...)
python3 luman.py doctor    # validate: dangling routes, missing files, orphan modules
python3 luman.py help      # all commands
```

## Why `doctor` matters

`doctor` is the thing the old architecture fundamentally could not do: prove the
system is internally consistent. It fails the build if a command points at a
missing file, a section references an unknown module, or a module is declared
but unused. Wire it into a GitHub Action and the system polices its own
integrity forever.

## Status

Skeleton / proof of architecture. Active sections render and validate; `music`
and `life` are intentionally declared-but-unbuilt to show how `doctor`
distinguishes "planned" from "broken".

## Next decisions

1. **Runtime** — does this stay a CLI, become a local web UI, or back a ChatGPT
   GPT / Claude agent? The manifest works for all three.
2. **Migration** — port one real section end-to-end (Harmonic Time is the best
   candidate) as the reference implementation, then bring the rest across.
