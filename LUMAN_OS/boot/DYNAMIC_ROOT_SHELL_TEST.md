# LUMAN Dynamic Root Shell Test — 2026-08-18

## Purpose

Verify that `/open luman` uses a stable navigation shell while delegating volatile current-state reconstruction to `/boot luman`.

## Overall Result

```text
PASS
```

## Test 1 — Root Owns Stable Interface

Expected:

- system identity remains in `ROOT_MENU.md`;
- constitutional framing remains in `ROOT_MENU.md`;
- navigation remains in `ROOT_MENU.md`;
- stable commands and operating laws remain in `ROOT_MENU.md`.

Result:

```text
PASS
```

## Test 2 — Boot Owns Volatile State

Expected:

`ROOT_MENU.md` must delegate the following to `/boot luman`:

- current mode;
- current strategic fronts;
- current Top 3;
- upcoming dates and commitments;
- current open loops;
- stale-state warnings;
- current project gates;
- recommended next move.

Result:

```text
PASS
```

The Root Shell explicitly defines a two-layer render:

```text
LAYER 1 — LIVE BOOT
LAYER 2 — STABLE SHELL
```

## Test 3 — No Hardcoded Live Dates

Expected:

No specific release date, appointment date, family-plan date, or other volatile date should be embedded as current truth in the Root Shell.

Result:

```text
PASS
```

## Test 4 — No Hardcoded Current Project State

Expected:

No current Top 3, current strategic-front names, current project gate, specific current open loop, or current warning should be embedded as live truth.

Result:

```text
PASS
```

## Test 5 — Root Command Delegation

Expected behavior for:

```text
/open luman
Open LUMAN
LUMAN
```

Flow:

```text
Root Shell
-> /boot luman
-> source-grounded live status
-> stable navigation
```

Result:

```text
PASS
```

## Test 6 — Existing Core Router Compatibility

Observed core route:

```text
/open luman -> LUMAN_OS/ROOT_MENU.md -> pull live priorities and open loops
```

The new Root Shell strengthens this route by requiring `/boot luman` before current-state presentation. Boot adds freshness checks, source conflict resolution, and authorized private-state retrieval while preserving the existing Root entry point.

Result:

```text
PASS — COMPATIBLE
```

## Test 7 — Privacy Boundary

Expected:

Boot may read authorized private state, but the Root Shell must not duplicate private details into the public repository.

Result:

```text
PASS
```

## Test 8 — Anti-Drift Rule

Expected:

The Root Shell explicitly prohibits future maintainers from embedding live dates, current Top 3, current fronts, private upcoming plans, current open loops, project gates, warnings, or current next moves.

Result:

```text
PASS
```

## Regression Fixed

Previous behavior allowed `ROOT_MENU.md` to function as a cached dashboard, which caused it to retain older shipping-state language after the authoritative operational sources had advanced.

New behavior:

```text
ROOT_MENU = stable shell
BOOT = live state
```

## Sovereignty Result

```text
PASS
```

Live-state reconstruction remains recommendation/orientation infrastructure. Edward retains authority over which priority or action becomes active.

## Next Gate

```text
Merge dynamic Root Shell
-> test correction / supersession / deletion behavior
-> expand operational skills
```
